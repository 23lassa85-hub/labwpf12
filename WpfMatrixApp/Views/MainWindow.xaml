<Window x:Class="WpfMatrixApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfMatrixApp.ViewModels"
        xmlns:conv="clr-namespace:WpfMatrixApp.Converters"
        mc:Ignorable="d"
        Title="Обработка матриц" 
        Height="700" Width="1100"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <conv:MatrixToDataTableConverter x:Key="MatrixToDataTableConverter"/>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>


        <conv:StringNotEmptyToVisibilityConverter x:Key="StringNotEmptyToVisibilityConverter"/>
    </Window.Resources>

    <DockPanel>

        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Файл">
                <MenuItem Header="Открыть..." Command="{Binding OpenFileCommand}" InputGestureText="Ctrl+O"/>
                <MenuItem Header="Сохранить как..." Command="{Binding SaveFileCommand}" InputGestureText="Ctrl+S"/>
                <Separator/>
                <MenuItem Header="Сгенерировать тестовый файл" Command="{Binding GenerateTestFileCommand}"/>
                <Separator/>
                <MenuItem Header="Обработать матрицы" Command="{Binding ProcessCommand}" InputGestureText="F5"/>
                <Separator/>
                <MenuItem Header="Выход" Command="{Binding ExitCommand}" InputGestureText="Alt+F4"/>
            </MenuItem>
        </Menu>


        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <Border Grid.Row="0" Background="#FFF8F8F8" BorderBrush="#FFDDDDDD" BorderThickness="0,0,0,1" Padding="10,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="Исходный файл:" Style="{StaticResource InfoTextStyle}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SourceFilePath}" Style="{StaticResource InfoTextStyle}" 
                                   TextTrimming="CharacterEllipsis" MaxWidth="400" Margin="5,2,0,2"
                                   ToolTip="{Binding SourceFilePath}"/>
                        <TextBlock Text="  |  " Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Text="Второй файл:" Style="{StaticResource InfoTextStyle}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SecondFilePath}" Style="{StaticResource InfoTextStyle}" 
                                   TextTrimming="CharacterEllipsis" MaxWidth="400" Margin="5,2,0,2"
                                   ToolTip="{Binding SecondFilePath}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="Матриц: " Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Text="{Binding SourceMatrices.Count}" Style="{StaticResource InfoTextStyle}" FontWeight="Bold"/>
                        <TextBlock Text="  |  Чётных разностей: " Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Text="{Binding SecondMatrices.Count}" Style="{StaticResource InfoTextStyle}" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>


            <TabControl Grid.Row="1" Margin="5">
                <TabItem Header="Исходный файл после обработки">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Матрицы из исходного файла (обратные заменены для чётных разностей)" 
                                   Style="{StaticResource HeaderTextStyle}"/>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding SourceMatrices}" Margin="5">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#FFCCCCCC" BorderThickness="1" Margin="0,0,0,10" Background="White">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Border Grid.Row="0" Background="#FFE8F0E8" Padding="5,3">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="Матрица" FontWeight="Bold" FontSize="12"/>
                                                        <TextBlock Text="  |  Разность диагоналей: " FontSize="12"/>
                                                        <TextBlock Text="{Binding DiagonalDifference, StringFormat=F4}" FontWeight="Bold" FontSize="12"/>
                                                        <TextBlock Text="  |  " FontSize="12"/>
                                                        <TextBlock Text="Чётная: " FontSize="12"/>
                                                        <TextBlock Text="{Binding IsEvenDifference}" FontWeight="Bold" FontSize="12"/>
                                                        <TextBlock Text="{Binding Status}" FontSize="11" Foreground="Red" 
                                                                   Margin="10,0,0,0" FontStyle="Italic"
                                                                   Visibility="{Binding Status, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                                                    </StackPanel>
                                                </Border>

                                                <DataGrid Grid.Row="1" 
                                                          ItemsSource="{Binding Data, Converter={StaticResource MatrixToDataTableConverter}}"
                                                          Height="150" MaxHeight="200"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </TabItem>

                <TabItem Header="Второй файл (чётные разности)">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Матрицы с чётной разностью диагоналей" 
                                   Style="{StaticResource HeaderTextStyle}"/>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding SecondMatrices}" Margin="5">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#FFCCCCCC" BorderThickness="1" Margin="0,0,0,10" Background="White">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <Border Grid.Row="0" Background="#FFF0E8E8" Padding="5,3">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="Матрица (чётная разность)" FontWeight="Bold" FontSize="12"/>
                                                        <TextBlock Text="  |  Разность диагоналей: " FontSize="12"/>
                                                        <TextBlock Text="{Binding DiagonalDifference, StringFormat=F4}" FontWeight="Bold" FontSize="12"/>
                                                    </StackPanel>
                                                </Border>

                                                <DataGrid Grid.Row="1" 
                                                          ItemsSource="{Binding Data, Converter={StaticResource MatrixToDataTableConverter}}"
                                                          Height="150" MaxHeight="200"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </TabItem>
            </TabControl>

            <Border Grid.Row="2" Background="#FFF8F8F8" BorderBrush="#FFDDDDDD" BorderThickness="0,1,0,0" Padding="10,5"
                    Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Обработка..." VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                    <ProgressBar Grid.Column="1" Value="{Binding ProgressValue}" IsIndeterminate="False"/>
                    <TextBlock Grid.Column="2" Text="{Binding ProgressValue, StringFormat={}{0:F0}%}" 
                               VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold" MinWidth="40"/>
                </Grid>
            </Border>

            <StatusBar Grid.Row="3">
                <StatusBarItem>
                    <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource StatusTextStyle}"/>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                    <TextBlock Text="WPF Matrix Processor v1.0" Style="{StaticResource StatusTextStyle}" Foreground="#FF888888"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </DockPanel>
</Window>
